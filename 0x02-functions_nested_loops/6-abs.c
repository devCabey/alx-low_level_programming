#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 *
 * @num: Integer value
 *
 * Return: the absolute value of input
 */

int _abs(int num)
{
	if (num >= 0)
	{
		return (num);
	}
	else
	{
		return (-num);
	}
}
